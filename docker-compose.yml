version: "3.8"

services:
  redis-server:
    image: redis:7-alpine
    container_name: redis-db
    volumes:
      - redis-data:/data

  web:
    build: .
    container_name: node-app
    ports:
      - "8080:3000"
    depends_on:
      - redis-server

volumes:
  redis-data:
